# A fixture for the tests of this module, loaded by the `manifest` helper in
# `tests.rs`.

unified = ["Example Unified Collection"]

separated = [
  "Example Collection",
  "Example Group/Another Example Collection",
]
