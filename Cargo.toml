[package]
name = "install-local-lyrics"
version = "0.0.0"
edition = "2021"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
itertools = "0.14.0"
pipe-trait = "0.4.0"
reflink = "0.1.3"
